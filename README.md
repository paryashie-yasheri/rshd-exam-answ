# Виды памяти в PostgreSQL

![Память в PostgreSQL](images/Pasted%20image%2020260618203220.png)

## Shared buffers
Разделяемое буферное пространство
- Хранит копии блоков данных, считанных из файлов данных
- Служит для минимизации числа операций обмена с диском
- Если нужного блока/страницы данных нет в SB, то он читается с диска и помещается в SB
- Совместно используется всеми фоновыми и пользовательскими процессами экземпляра
- Можно настроить через `shared_buffers`
- По умолчанию 128 МБ

## WAL buffers
- WAL -- Write Ahead Log
- Хранит информацию об изменениях данных в БД — записи XLOG
- Изменениям присваивается LSN
	- Log sequence number
- Эта информация используется для воссоздания актуального состояния данных в случае восстановления БД
- Настраивается через параметр `wal_buffers`

## TODO: Other buffers

## Tablespace
- Позволяют задать пути (директорию в файловой системе), где будут храниться объекты БД (вне PGDATA)
- Позволяют управлять физическим расположением объектов БД

### Стандартные табличные пространства
- `pg_default` — соответствуют директории base в PGDATA
	- По умолчанию здесь хранятся файлы данных, соответствующие объектам БД
- `pg_global` — соответствуют директории global в PGDATA
	- `pg_database`, `pg_authid`, `pg_tablespace`, некоторые другие каталоги и индексы

## CLOG buffers
- CLOG -- Commit Log
- Хранит данные о статусе проведения транзакций (e.g. "в процессе", "закончена", etc)
- Размер автоматически устанавливается СУБД
- Доступен серверным процессам

## Хз че еще надо тут рассказать помимо перечисленного


# Фоновые процессы в PostgreSQL

![Фоновые процессы PostgreSQL](images/Pasted%20image%2020260618221419.png)

## Процесс записи в БД (writer process / background writer)

- Переодически записывает измененные (заполненные, грязные) страницы из SB на диск в файлы данных
- Помечает записанные страницы чистыми
- Облегчает работу процесса checkpointer
- bgwriter_delay

## Работа с контрольными точками / checkpointer

- Контрольная точка — точки в последовательности транзакций, в которые произведена синхронизация результатов выполненных операций с файлами на диске
-  Создание контрольной точки:
	1) WAL-буфер синхронизируется с диском
	2) Грязные страницы записываются на диск
	3) Контрольная точка фиксируется в логах
- Контрольная точка создается, когда зполнен max_wal_size или через время checkpoint_timeout (по умолчанию — 300 секунд) — в зависимости от того, что будет раньше

## Loggin collector

- Записывает сообщения, отправленные в stderr, в лог-файлы
- Для работы нужно, чтобы был установлен параметр logging_collector

## Archiver
- Копирует созданные WAL-файлы в указанное место
- По умолчанию выключен

## Stats collector

- Служит для сбора различных статистических данных о БД
- Статистика хранится в промежуточных файлах и через них используется другими процессами
- Директория с временными файлами определяется параметром stats_temp_directory

```
pg_stat_*
pg_stat_database, ...
```

# Файловая структура и конфигурация PostgreSQL

## Файловая структура

- PGDATA — путь к директории данных кластера; установлена заранее
	- global — содеожит таблицы уровня кластера (pg_database)
	- pg_wal — директория с WAL-файлами
	- pg_xact — директория с данными о коммитах транзакций (CLOG)
	- base — файлы данных базы данных кластера
- Объектам БД соответствуют файлы данных:
	- если до 1Gb (по умолчанию) — объекту соответствует 1 файл данных
	- если >1Gb — объекту соответствуют файлы-сегменты
	- Находятся в $PGDATA/base в директории соответствующей бд

## Конфигурация 

### Базовые конфигурационные файлы:

Стандартный путь, по которому располагаются конфигурационные файлы: `/etc/postgresql/$version/main/`

**postgresql.conf** — базовый файл для хранения настроек
**postgresql.auto.conf** — динамически изменяемые настройки (через `alter system`)
**ph_hba.conf** — конфигурация подключений к БД
**pg_ident.conf** — файл отображения имен

#### postgresql.conf
- Базовый файл для хранения настроек
- Создается при работе initdb
- По умолчанию располагается в PGDATA
- Представляет из себя набор параметров (имя-значение):
	- search_path = 'someSchema, public'
	- shared buffers = 128MB
- Определяют значение для всего кластера

#### Изменение кофигурационных

1) postgresql.conf\/`ALTER SYSTEM`
2) `ALTER DATAVASE` -- изменить 1. на уровне базы данных
3) `ALTER ROLE` -- переписать 1. и 2. на уровне пользователя
4) `SET` -- изменить для сессии (e.g. `SET $param TO $value/DEFAULT`)
5) Параметры можно задать в команде запуска сервера БД -- для переписи параметров postgresql.conf

# Процессы в PostgreSQL

# Табличные пространства. Назначение. Способы организации и настройки

> См. первый билет для доп инфы

Табличные пространство можно задать для БД целиком:
```sql
CREATE DATABASE testDB
TABLESPACE newTablespace;
```
Tablespace можно изменять:
```sql
ALTER TABLE STUDENT
SET TABLESPACE pg_default;
```

## Зачем нужны табличные пространства?
- Можно контролировать что где хранится
- Критически важные объекты можно размещать на более быстрых физических дисках
- Если заканчивается место на жестком диске, можно использовать другой диск для размещения объектов

# Схема, особенности работы со схемами в PostgreSQL, search_path

## Схема
- Используются для логической группировки объектов в БД
- У каждой БД в постгрес есть схема public
- Полное имя объекта в Postgresql: `dbName.schemaName.objectName`

## search_path
- Объекты можно использовать без полного имени -- тогда используется search_path
- search_path -- последовательность схем, которая будет использована для идентификации объекта, когда используется неполное имя
- Схемы рассматриваются в порядке из search_path
- Первая схема из search_path -- используется для создания объектов -- текущая
- pg_temp и pg_catalog автоматически добавляются в search_path перед первой указанной схемой (порядок можно переопределить)
- Изменять search_path нужно осторожно, т.к. меняется контекст выполнения запросов

# Подключение к PostgreSQL. Особенности и реализация

Для подключения к БД роль должна быть:
- LOGIN
- Содержать привилегию CONNECT на нужную БД
- Разрешение в pg_hba.conf 

К template0 нельзя подключится (она резервная)

# Пользователи, роли, привилегии

## Пользовательские права
- Разным категориям пользователей должны предоставляться:
	-  Разные возможности (в зависимости от их потребностей)
	- Для управления различных объектов БД
- Возможности:
	- Обеспечение доступа (или выполнение другой операции) с таблицами, представлениями
	- Создание пользователей

## Привилегии
Привилегии -- определяют предоставляемые возможности 
Виды:
- Системные
	- Описывают возможность осуществления операций над БД
	- Для взаимодействия с объектами -- операции над различными объектами (контроль над данными в объектах БД)
		- С различными объектами связаны различные привилегии

## Роли
Привилегии отражают конкретную возможность
Роли -- именованные наборы привилегий
- Позволяют управлять объектами и БД на более "высоком" уровне
- Могут выступать в качестве пользователей

Роли конфигурируются на уровнен кластера:
- Назначение и привилегии могут отличаться для разных БД
- Имя роли -- уникально для кластера

## Пользователи

При установке кластера создается администратор (postgres)
Созданы для отображения именованных пользователей системы
Могут быть созданы суперпользователем и пользователем с ролью `CREATEROLE`

Создание пользователя аналогично созданию роли с параметром LOGIN:
```sql
CREATE USER s123xxx;
CREATE ROLE s123xxx WITH LOGIN;
```

При создании БД создается роль public:
- public назначается всем пользователям и ролям -- определяет права пользователей и ролей по умолчанию к разным объектам

# Работа с ролями. INHERIT, NOINHERIT

- `WITH GRANT OPTION` -- указывает возможность дальнейшей передачи роли от того, кому она назначена
	- e.g.: `GRANT UPDATE ON STUDENT TO s123123 WITH GRANT OPTION`
- INHERIT/NOINHERIT -- наследование привилегий при работе с группами
- `set role`


> [!Important]
> Удаление группы не удаляет ее участников

# Установление соединения в PostgreSQL. Привилегии, роли, пользователи

> см. прошлые билеты

# PostgreSQL: template0, template1, назначение, особенности работы

- Изначально, template1 и template0  идентичны
- template0 -- служит в виде резервной копии
- template1 -- используется в качестве шаблона при создании других БД
- Можно создать свою БД, которая будет использоваться в качестве шаблона
- Если внести изменения в template1 -- они будут применены и для баз, созданных на его основе
- К template0 нельзя подключится (она резервная)

# Транзакции. Назначение. ACID

## General
- Транзакции объединяют последовательность действий в одну операцию
- Промежуточные состояни внутри последовательности операций не видны другим транзакциям
- Если что-то помешает успешно завершить транзакцию, ни один из результатов этих действий не сохранися в БД

## ACID
- Atomicy
	- Гарантирует, что результаты работы транзакции не будут зафиксированы в системе частично
	- Будут выполнены все операции в транзакции, либо не выполнено ни одной
- Consistency
	- После выполнения транзакции БД должна быть в согласованном (**целостном**) состоянии
	- Во время выполнения транзакции согласованность не требуется
- Isolation
	- Во время выполнения ранзакции другие транзакции (выполняющиеся параллельно) не должны оказывать влияние на результат транзакции
- Durabilty
	- При успешном завершении транзакции результаты ее работы должны остаться в системе независимо от возможных сбоев оборудования, системы итд

## Виды транзакций
- Неявные
- Явные
	- Транзакции, которые пользователь задает самостоятельно (`BEGIN`, `COMMIT`)
- По дефолту транзакции неявные

# Восстановление данных. Подходы (steal/no-steal, force/no-force). ARIES

## General
- Для предотвращения ситуаций, связанных с появлением несогласованных данных можно использовать **журнал**/**лог**
- СУБД сохраняет информацию об изменениях и фиксациях данных в журнале:
	- Первоначально запись сохраняется в соответствующем буфере (буфере журнала)
	- Буфер журнала синхронизируется с файлами журнала на диске (WriteLog)

## UNDO/REDO Logs

### UNDO Log
- Журнал отмены — это один из вариантов ведения журналов
- Возможные записи в журнале отмены:

| Команда                       | Описание                                                      |
| ----------------------------- | ------------------------------------------------------------- |
| BEGIN Tx                      | Начало транзакции                                             |
| Tx: R, old_val                | Изменение:<br>Транзакция Tx переписывает значение old_var в R |
| COMMIT Tx                     | Успешное завершение транзакции                                |
| ABORT Tx                      | Преждевременное завершение транзакции                         |
| START CHKn: $T_n, \dots, T_m$ | Начало создания контрольной точки                             |
| END CHKn                      | Завершение создания контрольной точк                          |

#### Правила записи в UNDO LOG
- Запись изменения данных (Tx: R, old_val) записывается в журнал (на диск) ДО сохранения обновленного значения на диске
- `COMMIT Tx` должен быть добавлен в журнал **ПОСЛЕ** обновления всех файлов данных, связанных с изменениями данной транзакции (после синхронизации буферного пространства данных с файлами данных)

#### Использование журнала отмены для восстановления данных
1) СУБД сканирует журнал от новых записей до старых. Отдельно фиксируются транзакции:
	- Группа 1:
		- Для которых есть запись COMMIT Tx
	- Группа 2:
		- Транзакции, для которых есть запись ABORT Tx, незавершенные транзакции
2) Когда в журнале встречается запись изменения (Tx: R, old_val), анализируется Tx. Если Tx:
	- Из группы 1:
		- Действия не предпринимаются
	- Из группы 2: 
		- Для R восстанавливается old_var (происходит отмена изменений)
3) СУБД записывает ABORT Tx для каждой незавершенной транзакции из группы 2
4) Запускается операция WriteLog, чтобы записи вида ABORT Tx оказались в журнале

### Контрольные точки

#### Создание контрольной точки
1. В журнал добавляется START CHKn: $T_{n}, \dots, T_{m}$
2. Ожидание завершения транзакций, которые работали в момент создания контрольной точки($T_{n}, \dots, T_{m}$)
	- В это время могут начинаться другие транзакции
3. В журнал добавляется END CHKn, фиксируется на диске через WriteLog
4. Записи перед START CHKn, у которой есть END CHKn, могут быть удалены

<!-- господи я устал босс, анду и реду логи это пиздец -->

## REDO LOG

TODO

## Steal/No-steal & Force/No-force

| Метод        | Описание                                                                                                                                                 | Используется в PostgreSQL |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------- |
| **no-steal** | — подход, при котором страница из буфера данных не может быть записана на диск до завершения транзакции, в ходе которой она была изменена                | ❌                         |
| **steal**    | — если позволено записывать в файлы данных данные еще не завершенных транзкций (для оптимизации запросов к диску или если буферная память заканчивается) | ✅                         |
| **force**    | — если требуется, чтобы все страницы, измененные транзакцией, записывались сразу перед COMMIT                                                            | ❌                         |
| **no-force** | — изменения, осуществленные завершенными транзакциями могут быть не зафиксированы на диске                                                               | ✅                         |

## ARIES

### -- Algorithms for Recovery and Isolation Expoiting Semantics a.k.a. Алгоритм  для восстановления данных

### Базируется на:
- WAL
- steal
- no-force

### Этапы
1) Анализ
	- Поиск "грязных" страниц и незавершенных транзакций
2) REDO
	- Повторение всех действий
3) UNDO
	- Откат действий незавершенной транзакции

> [!Important]
> Для работы требуется LSN, таблица транзакций, таблица "грязных" страниц

# Восстановление данных. Контрольные точки

(см. другой билет)[[[## Работа с контрольными точками / checkpointer]]] // TODO: добавить ссылку

# UNDO-журнал


TODO: добавить ссылку на уже расписанный анду

# REDO-журнал

TODO: добавить ссылку на еще не расписанный билет (но заголовок с тудушкой там уже есть)

# UNDO журнал по транзакции



# Разбор транзакции с BEGIN INSERT UPDATE COMMIT (UNDO/REDO журнал: Действия, Диск, Записи в логах)

# REDO журнал по транзакции

# VACUUM

## MVCC
MVCC -- Multi-Version Concurrency Control
> В PostgreSQL -- Serializable Snapshot Isolation (SSI)

Особенности MVCC (SI):
- При изменении данных в транзакции -- создается новая копия данных
- Существующие копии данных **не изменяются** $	o$ с накопленными неактуальными данными нужно что-то делать $	o$ их стоит удалять, т.к. они занимают место на диске. Для удаления таких записей используется **VACUUM**

Режимы работы вакуума:
- Дефолтный
- VACUUM FULL
- VACUUM FREEZE

# Master/master репликация

TODO (нет в лекциях)

# Ступенчатая (каскадная) репликация

![Каскадная репликация](images/Pasted%20image%2020260618214342.png)

# Недостатки асинхронной репликации

Могут потеряться данные хзз

TODO(нормально в лекциях про недостатки не рассказывается)

# Логическая репликация

# Репликация без ведущего узла. Особенности работы и взаимодействия

# Физическая репликация. Способы организации в Postgres

# Триггерная репликация

# Синхронная и асинхронная репликация

# Конечная согласованность. Задержка репликации

# Настройка физической репликации. PostgreSQL wal_keep_size, слоты


# Понятие масштабирования. Вертикальное масштабирование

TODO (нет в лекциях)

# Асимметричное шардирование. Способы решения данной проблемы

# Понятие масштабирования. Горизонтальное масштабирование

# Перебалансировка при шардировании. Методы перебалансировки

# Шардирование данных. Назначение. Базовые понятия

# BASE

# CAP теорема

# Виды аномалий при параллелизме

# Виды и особенности конфликтов при параллельном доступе

# High Availability
